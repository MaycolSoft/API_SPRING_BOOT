# PUERTO DONDE VA A CORRER NUESTRA API
server.port=8080

# CONFIGURACION PARA LA CONEXION A LA BASE DE DATOS
spring.datasource.url=jdbc:postgresql://10.0.0.10:5432/db_abacoweb_desarrollo
spring.datasource.password=desarrollo
spring.datasource.username=uabaco
spring.datasource.platform=postgresql

# URL RAIZ DEL PROYECTO
server.servlet.context-path=/api.abacus

# CONFIGURACION PARA EVITAR EL COORS
management.endpoints.web.cors.allowed-origins=*


## Hibernate Properties

# The SQL dialect makes Hibernate generate better SQL for the chosen database
spring.jpa.properties.hibernate.dialect = org.hibernate.dialect.PostgreSQLDialect

# Hibernate ddl auto (create, create-drop, validate, update)
spring.jpa.hibernate.ddl-auto = update

#Error cuando intenta conectar a DB, createClob is false
#Deshabilitar la creaci�n de LOB contextual como m�todo createClob ()
spring.jpa.properties.hibernate.temp.use_jdbc_metadata_defaults=false
